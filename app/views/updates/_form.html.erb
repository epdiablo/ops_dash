<%= form_for(@update) do |f| %>
  <% if @update.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@update.errors.count, "error") %> prohibited this update from being saved:</h2>

      <ul>
      <% @update.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  	<%= f.hidden_field :user_id, :value => current_user.id%>
  	
  </div>
  <div class="field">
  <%= f.hidden_field :ticket_id, :value => params[:ticket_id] %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :category %><br />
    <%= f.select :category, (["Update", "Note" ].map {|i| [i,i] }), :selected => "Update" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
